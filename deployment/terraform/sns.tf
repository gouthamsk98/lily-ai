# SNS Platform Application for Android Push Notifications
# Uncomment and configure after setting up Firebase Cloud Messaging
# resource "aws_sns_platform_application" "gcm" {
#   name                = "${var.project_name}-android"
#   platform            = "GCM"
#   platform_credential = var.fcm_server_key
# }
